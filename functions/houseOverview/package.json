{
  "name": "@maxfrise-cloud/houseoverview",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "tsc && vitest run --coverage",
    "test:watch": "vitest",
    "prebuild": "rm -rf dist",
    "build": "esbuild src/index.ts --bundle --sourcemap --external:@aws-sdk/* --platform=node --target=es2020 --outfile=dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
