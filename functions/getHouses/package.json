{
  "name": "@maxfrise-cloud/gethouses",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test:watch": "vitest",
    "test": "tsc && eslint && vitest --coverage",
    "prebuild": "rm -rf dist",
    "build": "esbuild src/index.ts --bundle --sourcemap --external:@aws-sdk/* --platform=node --target=es2020 --outfile=dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
